After knitting and rendering `fireSense_ELFs.Rmd`, overwrite this file with a symlink to `fireSense_ELFs.md`.

E.g., on Linux/macOS from terminal:

```bash
cd /home/emcintir/GitHub/FireSenseTesting/modules/fireSense_ELFs
rm README.md && ln -s fireSense_ELFs.md README.md
```
